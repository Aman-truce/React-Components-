import React from 'react'

/*
  Exercise 13: Positioning logic
  --------------------------------
  Place a button at the bottom of the screen and one at the top. Clicking
  each shows a 150px box below it, but if there isn't room below, it flips
  above.

  Done when: the bottom button's box appears above it and the top button's
  box appears below it, automatically.
*/

export default function PositionedBox() {
  // TODO: buttonRef, isOpen state, placement state ('above' | 'below')
  // TODO: on click, measure button with getBoundingClientRect(), compare to window.innerHeight
  return null
}
