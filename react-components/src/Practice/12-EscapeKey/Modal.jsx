import React from 'react'

/*
  Exercise 12: Escape key
  --------------------------------
  Build a simple Modal that opens with a button and closes on Escape.
  Also close it on outside click by reusing what you did in #11.

  Done when: both mouse and keyboard close it, and both listeners clean up
  on unmount.
*/

export default function Modal() {
  // TODO: isOpen state, "Open" button
  // TODO: useEffect for keydown listener (Escape -> close), cleaned up on unmount/close
  // TODO: reuse outside-click logic from exercise 11
  return null
}
