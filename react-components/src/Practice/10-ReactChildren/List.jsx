import React from 'react'

/*
  Exercise 10: React.Children
  --------------------------------
  Build <List> that numbers its children automatically by injecting an
  index prop into each one with React.Children.map and cloneElement.

  Done when: it works with one child, many children, and zero children
  without crashing.
*/

export default function List({ children }) {
  // TODO: React.Children.map(children, (child, index) => React.cloneElement(child, { index }))
  return null
}
